import { PokemonIni } from 'interfaces/pokemonType';

export type UserType = {
  berries: number,
  bestiary: number[],
  mail: string,
  mainPoke: PokemonIni,
  money: number,
  name: string,
  pvpTotal: number,
  pvpWin: number,
  rating: number,
  registered: any
};
